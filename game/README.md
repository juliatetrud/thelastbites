# game/

Production game code lives here.

`index.html` is the game. Open it in a browser to play. No build step, no server required.

---

The prototype in `../prototype/` is reference only — do not edit it.

See `../design-docs/` for the full design documentation.
See `../sprints/` for sprint specs.
